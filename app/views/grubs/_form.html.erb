<%= form_for(@grub) do |f| %>
  <% if @grub.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@grub.errors.count, "error") %> prohibited this grub from being saved:</h2>

      <ul>
      <% @grub.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :energy %><br>
    <%= f.number_field :energy %>
  </div>
  <div class="field">
    <%= f.label :date %><br>
    <%= f.date_select :date %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
